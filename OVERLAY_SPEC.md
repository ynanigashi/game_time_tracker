# Overlay Display Spec (Draft v2)

## 目的
- `today_time_display` が実際に視認できない時だけ、同じ値を最小オーバーレイで補完表示する。
- メインウィンドウが視認可能な時は、オーバーレイを表示しない。

## 対象表示
- 表示内容: `today_time_display` に表示されている時刻文字列（例: `01:23:45.6`）
- 表示スタイル: 半透明・最前面・クリック透過・フォーカス非奪取
- 手動制御: 提供しない（ユーザーがオーバーレイを直接移動/閉じる手段は持たせない）

## 表示位置
- オーバーレイ位置・サイズは `today_time_display` のグローバル座標と一致させる。
- メインウィンドウ移動・リサイズ時も追従する（UI tick で同期）。

## 表示条件
以下をすべて満たす場合のみ表示。

1. メインウィンドウが可視（`isVisible=True`）
2. メインウィンドウが最小化されていない（`isMinimized=False`）
3. メインウィンドウが非アクティブ（`isActiveWindow=False`）
4. `today_time_display` のサンプル点が他ウィンドウに覆われている

補足:
- 条件3は条件4と一部重複するが、早期リターンで不要な Win32 判定を減らし、フォーカス遷移中の瞬間的な誤判定を抑えるために残す。

## 非表示条件
- 表示条件を1つでも満たさない場合は非表示。
- 特に次は常に非表示:
  - メインウィンドウが前面（アクティブ）
  - メインウィンドウが最小化
  - 非アクティブでも `today_time_display` が露出している

## 被り判定（Windows）
### 判定点
- `today_time_display` の5点を使う:
  - 中心
  - 左上寄り
  - 右上寄り
  - 左下寄り
  - 右下寄り
- 「寄り」の定義:
  - `x = left + width * 0.25` / `x = left + width * 0.75`
  - `y = top + height * 0.25` / `y = top + height * 0.75`
  - 角そのものは使わず、丸角・影領域を避けるため内側25%点を使う。
- 5点中1点でも「他ウィンドウが最前面」なら「被りあり」と判定。

### 判定手順
1. 各サンプル点で `WindowFromPoint` を呼ぶ
2. 返却HWNDが「自ウィンドウ集合」に属する場合、`GetWindow(..., GW_HWNDNEXT)` で背面を辿る
3. 自ウィンドウ以外のHWNDが見つかればその点は「被り」
4. いずれかの点が被りならオーバーレイ表示

### 自ウィンドウ集合
- 現時点では以下を自ウィンドウとして除外:
  - メインウィンドウ（`MainWindow`）
  - オーバーレイウィンドウ（`TodayTimeOverlayWindow`）
- 将来ウィンドウ（設定ダイアログ等）が増える場合は、同集合に追加する。

### Z-order探索のガード
- `GW_HWNDNEXT` の探索は最大 `MAX_Z_WALK` 回で打ち切る（無限ループ防止・負荷上限化）。
- 打ち切り時は「被りなし（非表示）」扱いにする（誤表示を避ける保守側の判定）。
- 初期値は `MAX_Z_WALK = 32` を採用し、実環境で打ち切り頻度を確認する。

### 既知の限界
- `TOPMOST`/非`TOPMOST` バンド混在環境では `GW_HWNDNEXT` による探索が期待通りに全ケースをカバーしない可能性がある。
- 本仕様では「誤表示を減らす」ことを優先し、判定不能ケースは非表示寄りに倒す。

## 更新タイミング
- `UI_REFRESH_INTERVAL_SECONDS` ごとの `_ui_tick` で以下を実行:
  1. 時刻文字列同期
  2. ジオメトリ同期
  3. 表示/非表示判定

## パフォーマンス方針
- 判定は「非アクティブ時のみ」実行する。
- 1 tick あたりの Win32 呼び出し回数に上限を設ける（5点 × `MAX_Z_WALK`）。
- 0.1秒更新で負荷問題が出る場合は、被り判定のみ 0.2〜0.5秒に間引く。

## OS依存・制約
- Windows 向け仕様（Win32 API 依存）。
- 透明ウィンドウ/特殊描画ウィンドウ/外部オーバーレイでは、見え方とヒットテスト結果が一致しない場合がある。

## マルチモニタ
- `today_time_display` のグローバル座標追従自体はマルチモニタでも動作対象。
- 非目標は「高度なモニタ依存最適化」:
  - モニタごとの DPI 差異に対する厳密補正
  - モニタ跨ぎ時の高度な優先順位制御

## 非目標
- 最小化中にオーバーレイのみ残す動作（最小化時は非表示）。
- オーバーレイの手動ドラッグ/手動クローズ UI の提供。

## 受け入れ確認（手動）
1. メインウィンドウが前面: オーバーレイは出ない。
2. メインウィンドウを非アクティブにするが `today_time_display` が見えている: 出ない。
3. `today_time_display` の一部を他ウィンドウで隠す: 出る。
   - 5点サンプルの少なくとも1点が覆われる程度に隠す。
4. 隠しを解除する: 消える。
5. オーバーレイ表示中にメインウィンドウをアクティブ化: 消える。
6. メインウィンドウを最小化: 消える。
7. メインウィンドウを閉じる: オーバーレイも閉じる。
8. メインウィンドウ移動/リサイズ: オーバーレイ位置・サイズが追従する。
9. 高DPI環境（125%/150%）で座標ズレがない。
10. `MAX_Z_WALK` 打ち切りが頻発しない（必要に応じてログ確認）。

## 未解決/要確認ポイント
- 透明ウィンドウや外部常駐オーバーレイを「被り」とみなすかの最終ルール。
- `MAX_Z_WALK=32` の妥当性（実環境での調整余地: 16 / 64 など）。
